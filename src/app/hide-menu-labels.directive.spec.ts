import { HideMenuLabelsDirective } from './hide-menu-labels.directive';

describe('HideMenuLabelsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideMenuLabelsDirective();
    expect(directive).toBeTruthy();
  });
});
